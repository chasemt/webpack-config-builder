# Use the public npm registry
registry=https://registry.npmjs.org/

# Use auth token from environment (do NOT commit tokens)
# Set NPM_TOKEN as an environment variable in CI instead of checking in
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Produce deterministic installs
save-exact=true
package-lock=true

# Less noisy logs and faster CI installs
progress=false
loglevel=warn
scripts-prepend-node-path=auto

# Enforce SSL for registry connections
strict-ssl=true
# {{projectName}}

这是一个使用 `webpack-config-builder` 创建的 React 项目。

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

开发服务器将在 `http://localhost:3000` 启动。

### 生产构建

```bash
npm run build
```

构建文件将输出到 `dist/` 目录。

## 项目结构

```
{{projectName}}/
├── public/
│   └── index.html          # HTML 模板
├── src/
│   ├── App.tsx             # 主组件
│   └── index.tsx           # 入口文件
├── webpack.config.js        # Webpack 配置
├── tsconfig.json           # TypeScript 配置
└── package.json
```

## 更多信息

查看 [webpack-config-builder 文档](https://github.com/yourusername/webpack-config-builder) 了解更多配置选项。

